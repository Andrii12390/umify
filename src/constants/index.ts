export const PUBLIC_ROUTES = {
  SIGN_IN: '/',
  SIGN_UP: '/signup',
  VERIFICATION: '/verification',
} as const;

export const PRIVATE_ROUTES = {
  DIAGRAMS: '/diagrams',
  PROFILE: '/profile',
  SETTINGS: '/settings',
} as const;
